{
  "name": "sales-performance-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Sales performance dashboard: revenue trends, target vs. actual and tiered incentive calculator. Runs entirely in the browser.",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "express": "^5.2.1",
    "xlsx": "0.18.5"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
